<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo redondeado con borde -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="1dp"
            android:color="@color/black" /> <!-- Color del borde -->
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="50dp"/>
        </shape>
    </item>
    <!-- Triángulo indicador de lista desplegable -->
    <item android:right="15dp" android:top="8dp" android:bottom="8dp">
        <bitmap android:src="@drawable/flechaabajo"
            android:gravity="end|center_vertical" />
    </item>
</layer-list>

